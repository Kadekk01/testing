<!-- Built in pp -->
<!-- l=day's name, d=date, m=month in number, M=month in text, Y=year -->
<?php
    //for viewing date with certain format
    // echo date("l, d-m(F)-Y");
    // echo time();
    // echo mktime(0,0,0,0,0,0)
    // hour, minute, second, month, date, year
    // strtotime
    // echo date(strtotime("04 Jan 2022"))
?>

<!-- exam -->
<?php 
    // echo date("l, d F Y", time()-60*60*24*7344)
    // echo date("l, d F Y", mktime(0,0,0,11,26,2001));
    // echo date("l", strtotime("26 Nov 2001"))
?>

<?php
// string
// strlen(), to count lenght of string
// strcmp(), to compare 2 string
// explode(), to split a string into an array
// htmlspecialchars(), special string to prevent pranksters from entering the website
?>

<?php
//Utility
// var_dump(), to print a variable, array or object
// isset(), to check whether the variable has been created or not
// empty(), to check whether the existing variable is empty or not
// die(), to stop the program
// sleep(), to pause the program
?>